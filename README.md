```bash
 conda init
 conda env create -f env.yml
 conda activate env
 
```

---

### Техническое Задание (ТЗ)
**На разработку модели машинного обучения для оценки UI/UX веб-страниц**

### 1. 🎯 Общие сведения
* **Проект:** "UI/UX Evaluator"
* **Цель:** Разработать систему на базе нейронной сети, способную автоматически оценивать удобство, читабельность и понятность интерфейса веб-страницы.
* **Входные данные:** Структурированный `JSON`-файл, содержащий DOM-дерево, CSS-свойства и текстовое содержимое страницы.
* **Выходные данные:** Итоговая оценка "юзабилити" (например, по шкале от 0 до 100).
* **Основной стек:** Python, TensorFlow 2.x, Keras, TensorFlow Hub.

### 2. 📑 Задачи Модели
Модель должна анализировать входной `JSON` и на его основе оценивать:
* **Читабельность:** Контраст текста и фона, размеры и типы шрифтов, межстрочные интервалы.
* **Визуальная иерархия:** Насколько логично расположены элементы (`top`, `left`), как используются заголовки и отступы.
* **Интерактивность:** Наличие и понятность интерактивных элементов (кнопки, ссылки, поля ввода), используя их `text`, `alt`, `placeholder`.
* **Общая композиция:** Как совокупность всех элементов складывается в единый "пользовательский опыт".

### 3. 🧩 Архитектура Решения
Решение состоит из двух основных блоков: **Модуля подготовки признаков (Feature Engineering)** и **Основной Модели (Трансформера)**.

#### 3.1. Модуль подготовки признаков
Задача этого модуля — преобразовать каждый элемент из списка `"elements"` (т.е. каждый DOM-узел) в **единый плоский "мега-вектор"** (`feature_vector`). Этот вектор будет содержать всю информацию об элементе.

Для каждого элемента `element` в списке `elements`:

1.  **Текстовые признаки:**
    * **Поля:** `text`, `alt`, `title`, `placeholder`.
    * **Процесс:** Конкатенировать (склеить) текст из этих полей.
    * **Векторизация:** Подать полученную строку в предобученную модель `Universal Sentence Encoder` (из `tensorflow_hub`).
    * **Выход:** `vector_text` (размер **512**).

2.  **Числовые (скалярные) признаки:**
    * **Поля (Геометрия):** `width`, `height`, `top`, `left`. (Распарсить 'px', 'auto' -> 0).
    * **Поля (Стиль):** `fontSize`, `fontWeight`, `lineHeight`, `opacity`, `letterSpacing`.
    * **Поля (Иерархия):** `depth` (уровень вложенности, вычисляется рекурсивно), `num_children` (длина списка `children`).
    * **Процесс:** Собрать все эти числа в один вектор и **нормализовать** (например, `StandardScaler` или `MinMaxScaler`).
    * **Выход:** `vector_numeric` (размер ~**15-20**).

3.  **Цветовые признаки:**
    * **Поля:** `color`, `backgroundColor`.
    * **Процесс:** Преобразовать `rgb(r, g, b)` в `[R/255, G/255, B/255]`.
    * **Доп. признак:** Рассчитать **коэффициент контрастности WCAG** между `color` и `backgroundColor`. Это число (от 1 до 21) добавить в `vector_numeric`.
    * **Выход:** `vector_colors` (размер $3 + 3 = 6$).

4.  **Категориальные признаки:**
    * **Поля:** `tag` ('div', 'a', 'p'...), `position` ('static', 'relative'...), `display` ('block', 'inline'...), `textAlign` ('left', 'center'...).
    * **Процесс:** Для каждого поля создается словарь (e.g., `'div': 1, 'a': 2...`). Эти числовые индексы будут подаваться в `Embedding`-слои внутри Keras.

**Итог (Шаг 3.1):**
Входной `JSON` для одной страницы преобразуется в **матрицу** размером `(N, total_feature_size)`, где `N` — количество элементов на странице.

#### 3.2. Архитектура Нейронной Сети (Keras Functional API)
**Основная идея:** Мы рассматриваем страницу как "предложение", а каждый "мега-вектор" элемента — как "слово". Трансформер идеально подходит для поиска *взаимосвязей* между этими "словами" (элементами). Он сможет "понять", что кнопка с `top: 100` связана с полем ввода с `top: 102`, даже если они далеко в DOM-дереве.

1.  **Входной слой (Input):**
    * Принимает матрицу признаков, подготовленную на Шаге 3.1.
    * `Input(shape=(None, total_feature_size))`, где `None` — это `N` (переменное кол-во элементов).

2.  **Маскирование (Masking):**
    * `Masking(mask_value=0.0)`
    * *Примечание: Этот слой нужен, если мы будем дополнять (padding) страницы с разным количеством элементов до одной длины (например, до 500 элементов) нулями.*

3.  **Блок Трансформера-Энкодера:**
    * Это ядро модели. Он состоит из `MultiHeadAttention` (многоголовочное внимание) и `FeedForward` (полносвязная сеть).
    * `MultiHeadAttention(num_heads=8, key_dim=128)`: Слой, который "смотрит" на все элементы сразу и вычисляет, какие из них наиболее важны друг для друга (на основе их `feature_vector`).
    * `LayerNormalization` + `Dropout`: Стандартные слои для стабилизации обучения.
    * `Dense` (FeedForward): Полносвязный слой.
    * *Примечание: Этот блок может быть повторен 1-3 раза для углубления модели.*

4.  **Агрегация (Свертка):**
    * `GlobalAveragePooling1D()`
    * **Задача:** "Схлопнуть" матрицу элементов `(N, transformer_dim)` в один-единственный вектор `(1, transformer_dim)`, который представляет **всю страницу целиком**.

5.  **Классификатор ("Голова"):**
    * Обычная полносвязная сеть, которая принимает вектор страницы и выносит вердикт.
    * `Dense(64, activation='relu')`
    * `Dropout(0.3)`
    * `Dense(1, activation='linear')`: **Выходной слой**. Один нейрон, выдающий число (регрессия).

[Диаграмма архитектуры]
`JSON` -> `[Модуль подготовки признаков]` -> `(N, F)` матрица -> `[Input]` -> `[Transformer Block]` -> `[GlobalAveragePooling1D]` -> `[Dense Head]` -> **Оценка (0-100)**

### 4. 📊 Данные и Обучение

#### 4.1. Требования к данным
* Для обучения модели **критически необходимо** наличие **размеченного датасета**.
* Датасет должен состоять из пар `(page.json, target_score)`, где `target_score` — это экспертная оценка юзабилити (например, от 0 до 100), выставленная дизайнерами или UX-специалистами.

#### 4.2. Процесс обучения
* **Задача:** Регрессия (предсказание числа).
* **Функция потерь (Loss):** `MeanSquaredError` (MSE) или `MeanAbsoluteError` (MAE).
* **Метрики качества:** `MAE` (показывает, на сколько баллов в среднем ошибается модель).
* **Оптимизатор:** `Adam`.

### 5. 🚀 Ожидаемый результат
Готовая `model.h5` (или `SavedModel`) модель, которая принимает на вход `JSON`-представление страницы (в виде матрицы признаков) и возвращает `float`-значение ее юзабилити-оценки.

---

Этот документ можно использовать как основу для разработки. Он четко разделяет этапы подготовки данных (самый сложный) и самой архитектуры модели.

Хотите обсудить какой-либо из этих пунктов подробнее, например, как реализовать Модуль подготовки признаков (Шаг 3.1) на Python?